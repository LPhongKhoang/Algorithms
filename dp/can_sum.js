/**
 * Can sum (targetSum: number, arr: number[])
 * Return boolean indicates whether or not it is possible to generate 
 * the targetSum using numbers from the array
 *  - arr[i] >= 0
 *  - arr[i] can be used many time as you need
 */

/**
 * 
 * @param {number} targetSum 
 * @param {number[]} arr 
 * @returns boolean 
 * @description Way 1: Naive approach
 * @description Time: 
 * @description Space: 
 */
function naiveCanSum(targetSum, arr) {
  return true;
}

function main(){
  // naiveCanSum(7, [5, 3, 4, 7]); // true
  // naiveCanSum(7, [2, 4]); // false
};

main();